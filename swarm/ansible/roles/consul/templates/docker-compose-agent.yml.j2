version: '2'
services:
  agent:
    image: consul:v0.6.4
    command: agent -client=0.0.0.0 -retry-join={{ consul_master_ip }}
    restart: always
    ports:
      - "8300:8300"
      - "8400:8400"
      - "8500:8500"
      - "8600:53/udp"
    container_name: consul-agent
